// import foo from "dns";

// foo.lookup("smhi.se", function (e, addr) {
//   console.log(`Address(es): ${addr}`);
// });

import utils from "./utils.js";

console.log(`${utils.appName} v${utils.version}`);
